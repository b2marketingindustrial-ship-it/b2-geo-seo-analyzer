{
  "name": "@b2/scoring-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Motor de pontuação puro do B2 GEO/SEO Analyzer — facts → findings → score → action plan",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@b2/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}