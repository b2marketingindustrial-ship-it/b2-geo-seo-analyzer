{
  "name": "@b2/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Contratos compartilhados do B2 GEO/SEO Analyzer — facts, findings, scores, events e API DTOs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}