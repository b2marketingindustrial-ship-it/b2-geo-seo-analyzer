{
  "name": "@b2/extension",
  "version": "0.1.0",
  "private": true,
  "description": "B2 GEO/SEO Analyzer — Extensão Chrome Manifest V3",
  "scripts": {
    "build": "echo 'Extension built (manual) — manifest and src files ready'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3"
  }
}